import * as product from "./product";
import * as supplier from "./supplier";
import * as inventoryEntry from "./inventoryEntry";
import * as category from "./category";
import * as unit from "./unit";

const api = {
  product,
  supplier,
  inventoryEntry,
  category,
  unit,
};

export default api;
